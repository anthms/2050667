on:
  workflow_run:
    workflows: [Main]
    types:
      - completed

jobs:
  job-name:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo ${{ github.event.workflow_run.conclusion }}
          echo ${{ github.repository_owner }}
          echo ${{ github.ref }}
